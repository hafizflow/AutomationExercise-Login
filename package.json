{
  "name": "automationexercise-login",
  "version": "1.0.0",
  "description": "Playwright (JavaScript) automation of the login flow on automationexercise.com",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "dotenv": "^16.4.5"
  }
}
